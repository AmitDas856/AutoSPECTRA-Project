# AutoSPECTRA - CAN-bus intrusion detection (laptop-class, no GPU needed)
numpy>=1.26
scikit-learn>=1.3      # baseline classifier (RandomForest) + metrics
pandas>=2.0            # parse the HCRL Car-Hacking CSV files
matplotlib>=3.8        # ROC curves / confusion-matrix plots for the report
# Deep models (the headline CNN on CAN-images + LSTM). CPU build is fine.
# tested on torch 1.13 (python 3.7) and 2.x - anything >=1.13 works for TinyCNN
torch>=1.13
# Live demo dashboard for the pitch:
# streamlit>=1.30
